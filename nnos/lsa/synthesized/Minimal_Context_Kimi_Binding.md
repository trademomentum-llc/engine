# NEURODIOS MINIMAL CONTEXT (Kimi Binding — Token Optimized)

**Version:** 1.0 | **Scope:** Dual-root (engine/nnos + apps) | **Maintainer:** Grok

Load this when token budget is tight. For full detail load the long-form Chained_Source_of_Truth_Kimi_Binding.md.

---

## 1. 8 Validated Denominators (Core Physics)

1. Fluctuation Dynamics (velocity/accel/duration)
2. Budget/Resource Accounting (incl. Compute Footprint sub-budget)
3. Contrast Differential
4. Controlled Oscillation
5. Adaptation Offset
6. Primitive Traceability (Atomic Dependency Mapping)
7. Origin Vault (provenance + state history)
8. Drift Detection

All mechanisms must be expressed via combinations of these 8.

---

## 2. Mandatory Criteria (apply to everything)

- Atomic + Kinetic + Fractal primitives
- Relationships **earn** mechanisms
- Mechanisms deliver deterministic real-world uplift + compounding solutions to actual user problems
- Maintain healthy lifestyle even during deliberate Roller Coaster cycles for uplift
- Prefer smallest safe data type (INT8/INT16 over FP32 wherever possible)

---

## 3. Architecture Snapshot (Ultra Compact)

- **Kernel**: Jasterish Micro-Kernel (Phase 2 complete: IDT, VFS, ELF, CoW, higher-half)
- **Health Governor**: NeuroBalance Engine (synthesized from nnos-lsa) — enforces the 8 denominators + minimal compute paths
- **Uplift Engine**: Roller Coaster (stress → relief) + active Solution Generation + Improvement Compounding during Crystallization
- **nnos-lsa**: Fully incorporated (LSA/TP-HCF, Origin Vault, primitive taxonomies). All material filtered through the 8 denominators.

---

## 4. Dual-Root Rule

- Working in **apps/** (compiler/toolchain)? Load `apps/context/Chained_Source_of_Truth_Kimi_Binding.md` (or this minimal file).
- Working in **engine/nnos/**? Load the canonical in `engine/nnos/lsa/synthesized/`.
- Both roots share the same 8 denominators and criteria. Compiler outputs must be compatible with kernel expectations defined by these denominators.

---

## 5. Efficiency Mandate (New)

Every action/offset/primitive evaluation **must** use the smallest safe integer type.

- Default: INT8 (0-255 normalized) or INT16 where more headroom needed.
- FP32 only when physics genuinely requires it (rare in behavioral control).
- NeuroBalance Coordinator now tracks "Compute Footprint" as a first-class sub-resource inside Budget Accounting.
- Prefer actions with lower `compute_action_footprint()` score when multiple options exist.

Token rule for this context: This file is the compressed form. Use the long-form binding only when you need the full rationale or historical decisions.

---

**End of Minimal Context Binding** (≈ 380 tokens)